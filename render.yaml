services:
  - type: web
    name: vision-app
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    # Add your OPENAI_API_KEY in Render dashboard under Environment Variables
